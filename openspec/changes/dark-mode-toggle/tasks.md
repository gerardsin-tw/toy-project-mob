## 1. index.html – Theme Infrastructure

- [ ] 1.1 Add CSS custom properties for light and dark themes to the `<style>` block in `index.html`
- [ ] 1.2 Replace hard-coded colour values in `index.html` styles with the CSS variables
- [ ] 1.3 Add toggle button markup fixed to the top-right corner in `index.html`
- [ ] 1.4 Add `<script>` block to `index.html` that restores the saved theme from `localStorage` on page load
- [ ] 1.5 Add JS click handler to the toggle button in `index.html` that switches the theme class on `<body>`, updates the button label, and saves the preference to `localStorage`

## 2. quiz.html – Theme Infrastructure

- [ ] 2.1 Add CSS custom properties for light and dark themes to the `<style>` block in `quiz.html`
- [ ] 2.2 Replace hard-coded colour values in `quiz.html` styles with the CSS variables
- [ ] 2.3 Add toggle button markup fixed to the top-right corner in `quiz.html`
- [ ] 2.4 Add `<script>` block to `quiz.html` that restores the saved theme from `localStorage` on page load
- [ ] 2.5 Add JS click handler to the toggle button in `quiz.html` that switches the theme class on `<body>`, updates the button label, and saves the preference to `localStorage`

## 3. Verification

- [ ] 3.1 Manually verify that clicking the toggle on the index page switches to dark mode and the label updates
- [ ] 3.2 Manually verify that the dark theme persists after a page reload on the index page
- [ ] 3.3 Manually verify that navigating to the quiz page preserves the chosen theme
- [ ] 3.4 Manually verify that all text and card elements have sufficient contrast in dark mode
